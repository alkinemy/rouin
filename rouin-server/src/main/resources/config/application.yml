spring:
  application:
    name: rouin
server.port: 10180

rouin:
  plaid:
    environment: DEVELOPMENT # SANDBOX, DEVELOPMENT, PRODUCTION
    client-id: ${PLAID_CLIENT_ID}
    secret: ${PLAID_SECRET}
    version: 2020-09-14